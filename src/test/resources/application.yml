server:
  port: 8081
  tomcat:
    uri-encoding: UTF-8

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.222.29:3306/eas?characterEncoding=utf-8&useUnicode=true
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

 #Hibernate ddl auto (validate|create|create-drop|update)

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
